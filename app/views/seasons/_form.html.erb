<%= form_for(@season) do |f| %>
  <% if @season.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@season.errors.count, "error") %> prohibited this season from being saved:</h2>

      <ul>
      <% @season.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :serial_id %><br>
    <%= f.select(:serial_id, options_from_collection_for_select(@serial, :id, :name)) %>
  </div>
  <div class="field">
    <%= f.label :season_number %><br>
    <%= f.number_field :season_number %>
  </div>
  <div class="field">
    <%= f.label :image_url %><br>
    <%= f.text_area :image_url, cols: 100, rows: 1   %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name, cols: 100, rows: 1   %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description, cols: 100, rows: 1   %>
  </div>
  <div class="field">
    <%= f.label :story %><br>
    <%= f.text_area :story, cols: 100, rows: 10   %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
